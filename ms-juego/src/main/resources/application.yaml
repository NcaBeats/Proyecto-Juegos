spring:
  application:
    name: ms-juego

  datasource:
    url: jdbc:h2:mem:db_juegos
    driver-class-name: org.h2.Driver
    username: postgres
    password: 1234

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    org.flywaydb: DEBUG

server:
  port: 8080
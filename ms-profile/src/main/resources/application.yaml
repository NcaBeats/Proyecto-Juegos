spring:
  application:
    name: ms-profile

  datasource:
    url:  jdbc:h2:mem:db_profiles
    driver-class-name: org.h2.Driver
    username: postgres
    password: 1234
  #  jdbc:postgresql://localhost:5432/db_usuarios
  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

#  flyway:
#    enabled: true
#    locations: classpath:db/migration
#
#logging:
#  level:
#    org.flywaydb: DEBUG

server:
  port: 8082
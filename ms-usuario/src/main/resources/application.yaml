spring:
  application:
    name: ms-usuario

  datasource:
    url: jdbc:postgresql://localhost:5433/db_usuario
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 1234

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: false
    baseline-on-migrate: true

logging:
  level:
    org.flywaydb: DEBUG

server:
  port: 8081